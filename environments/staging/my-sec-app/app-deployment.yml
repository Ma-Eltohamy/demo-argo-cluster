---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaivy-deployment
  namespace: foo
  labels:
    app: kaivy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: kaivy
  template:
    metadata:
      labels:
        app: kaivy
    spec:
      containers:
      - name: kaivy-cont
        image: kaivy
